PORT=5000
GITHUB_API_TOKEN=
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# IMPORTANT: Get a real GitHub Personal Access Token
# 1. Go to https://github.com/settings/tokens/new
# 2. Create a token with 'public_repo' scope
# 3. Replace the empty GITHUB_API_TOKEN value above
# Without a token, rate limits are very restrictive (60 req/hour)
